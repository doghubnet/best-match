import { useEffect, useState } from 'react';
const defaults=['Program scan','Document checklist','Financial scan','Interview practice','Final report'];
export default function Tasks(){const [done,setDone]=useState<boolean[]>([false,false,false,false,false]);useEffect(()=>{const s=localStorage.getItem('brovi_tasks'); if(s) setDone(JSON.parse(s));},[]);useEffect(()=>{localStorage.setItem('brovi_tasks',JSON.stringify(done));},[done]);const pct=Math.round(done.filter(Boolean).length/done.length*100);return <div className='card'><h1 className='text-3xl'>Tasks</h1><div className='mt-3 h-2 bg-slate-800 rounded'><div className='h-2 bg-indigo-500 rounded' style={{width:`${pct}%`}}/></div><p className='text-sm mt-2'>{pct}% complete</p><ul className='mt-3 space-y-2'>{defaults.map((t,i)=><li key={t}><label className='flex gap-2'><input type='checkbox' checked={done[i]} onChange={e=>{const n=[...done];n[i]=e.target.checked;setDone(n);}}/>{t}</label></li>)}</ul></div>}
