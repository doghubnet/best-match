import { useState } from 'react';
export default function DocumentScan(){const [r,setR]=useState<any>(null);return <div className='space-y-4'><form className='card space-y-3' onSubmit={e=>{e.preventDefault();setR({missing:['Issuer stamp'],quality:['Readable upload'],risks:['One checklist gap'],nextSteps:['Re-upload stamped copy']})}}><h1 className='text-3xl'>Document Scan</h1><select className='bg-slate-800 p-3 rounded'><option>passport</option><option>transcript</option><option>bank statement</option><option>admission letter</option><option>motivation letter</option><option>recommendation</option><option>other</option></select><input type='file'/><p className='text-sm text-slate-300'>Accepted: PDF, JPG, PNG, WEBP · Max size: 20MB</p><div className='card bg-slate-800/40'>Checklist: identity, dates, signatures, institution fields.</div><button className='btn'>Analyze document</button></form>{r&&<div className='card'><p>Missing items: {r.missing.join(', ')}</p><p>Quality notes: {r.quality.join(', ')}</p><p>Risks: {r.risks.join(', ')}</p><p>Next steps: {r.nextSteps.join(', ')}</p></div>}<p className='text-xs text-slate-400'>Safety: remove unnecessary sensitive data before upload when possible.</p></div>}
