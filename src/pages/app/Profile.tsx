import { useEffect, useState } from 'react';
export default function Profile(){const [f,setF]=useState<any>({name:'',education:'',country:'',program:'',sponsor:''});useEffect(()=>{const p=localStorage.getItem('brovi_profile');if(p) setF(JSON.parse(p));},[]);return <form className='card grid md:grid-cols-2 gap-3' onSubmit={e=>{e.preventDefault();localStorage.setItem('brovi_profile',JSON.stringify(f));}}><h1 className='text-3xl md:col-span-2'>Profile</h1>{[['name','Personal information'],['education','Education'],['country','Target country'],['program','Target program'],['sponsor','Sponsor details']].map(([k,p])=><input key={k} className='bg-slate-800 p-3 rounded' placeholder={p} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})} required/>)}<button className='btn md:col-span-2'>Save profile</button></form>}
