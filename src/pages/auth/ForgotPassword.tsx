import { useState } from 'react';
export default function ForgotPassword(){const [m,setM]=useState('');return <main className='container py-12'><form className='card max-w-xl space-y-3' onSubmit={e=>{e.preventDefault();setM('If this email exists, reset instructions have been sent.')}}><h1 className='text-3xl'>Forgot password</h1><input required className='bg-slate-800 p-3 rounded w-full' placeholder='Email'/><button className='btn'>Send reset link</button>{m&&<p>{m}</p>}</form></main>}
