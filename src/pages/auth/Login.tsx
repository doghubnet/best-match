import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { useAuth } from '@/lib/auth';
export default function Login(){const [email,setEmail]=useState('');const [e,setE]=useState('');const {login}=useAuth();const nav=useNavigate();return <main className='container py-12'><form className='card max-w-xl space-y-3' onSubmit={ev=>{ev.preventDefault(); if(!/.+@.+\..+/.test(email)) return setE('Enter valid email'); login(email); nav('/app');}}><h1 className='text-3xl'>Login</h1><input className='bg-slate-800 p-3 rounded w-full' value={email} onChange={v=>setEmail(v.target.value)} placeholder='Email'/><button className='btn'>Login</button>{e&&<p className='text-red-300'>{e}</p>}<Link to='/register'>Create account</Link><Link to='/forgot-password'>Forgot password</Link></form></main>}
