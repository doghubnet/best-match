import { useState } from 'react';import { Link, useNavigate } from 'react-router-dom';import { useAuth } from '@/lib/auth';
export default function Login(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');const {login}=useAuth();const nav=useNavigate();return <main className='container py-12'><form className='card max-w-lg space-y-3' onSubmit={e=>{e.preventDefault(); if(!/.+@.+\..+/.test(email))return setMsg('Invalid email'); login(email); nav('/app');}}><h1 className='text-3xl'>Login</h1><input className='w-full bg-slate-800 p-3 rounded' value={email} onChange={e=>setEmail(e.target.value)} placeholder='Email'/><button className='btn'>Continue</button>{msg&&<p>{msg}</p>}<Link to='/forgot-password'>Forgot password?</Link></form></main>}
