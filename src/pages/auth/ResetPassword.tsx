import { useState } from "react";
export default function ResetPassword(){const [message,setMessage]=useState("");const [error,setError]=useState("");return <main className="container py-14"><form className="card mx-auto max-w-md space-y-4" onSubmit={(event)=>{event.preventDefault();const form=new FormData(event.currentTarget);if(String(form.get("password"))!==String(form.get("confirm"))) return setError("Passwords do not match.");setError("");setMessage("Password updated in demo mode. If Supabase is configured, use the emailed reset link.");}}><h1 className="text-3xl font-bold">Reset password</h1><label className="block text-sm">New password<input name="password" className="input mt-2" type="password" minLength={8} required /></label><label className="block text-sm">Confirm password<input name="confirm" className="input mt-2" type="password" minLength={8} required /></label>{error&&<p className="text-red-300">{error}</p>}{message&&<p className="text-cyan-200">{message}</p>}<button className="btn w-full">Update password</button></form></main>}
